import React from "react";
import Home from "./pages/Home";

/**
 * The top-level component for the application.
 *
 * This component renders the Home component, which contains the main
 * application functionality.
 */
const App: React.FC = () => {
  return <Home />;
};

export default App;
